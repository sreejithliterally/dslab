def search(a,key):
	U = len(a)
	found =0
	i=0
	while i<U and found ==1:
		if a[i]==key
			found =1
			position = i
		i = i+1
	if found ==0:
		print("search unsuccesful")
	else:
		print("yes key is at"position)
	return(position)
		
		 
